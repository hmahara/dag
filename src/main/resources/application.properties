quarkus.web-bundler.dependencies.node-modules=node_modules
quarkus.http.port=2017
quarkus.native.resources.includes=templates/**, web/**

# application
greeting.message=Application in container
db.user=db-user-in-app-prop
db.password=db-password-in-app-prop

#quarkus.kubernetes-config.enabled=true
quarkus.kubernetes-config.config-maps=dag-config

# Map values in ConfigMap
#quarkus.helm.values.message.paths=(kind == ConfigMap).data.'hello.message'
#quarkus.helm.values.number.paths=(kind == ConfigMap).data.'hello.number'
#quarkus.helm.values.number.value-as-int=1
#quarkus.helm.values.flag.paths=(kind == ConfigMap).data.'hello.flag'
#quarkus.helm.values.flag.value-as-bool=true

#quarkus.kubernetes-client.trust-certs=false
#quarkus.kubernetes-client.namespace=dev